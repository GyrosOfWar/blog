[ ] Add pagination for views where it makes sense
[ ] Add routes for deleting, editing a post. (`DELETE /post/` and `GET/POST /post/{id}/edit` )
[ ] Is it really necessary to have a full-on multi-user blog thing?