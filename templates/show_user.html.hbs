<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Rocket: Cookie Examples</title>
  </head>
  <body>
    <h1>{{ user.name }}</h1>
    <p>
      {{#each posts.data as |p|}}
        <a href="/post/{{p.id}}">{{p.title}}</p
      {{/each~}}
    </p>
  </body>
</html>